{
    "root": "apps/gateway",
    "sourceRoot": "apps/gateway/src",
    "projectType": "application",
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "command": "make build",
          "cwd": "apps/gateway"
        }
      },
      "serve": {
        "executor": "nx:run-commands",
        "options": {
          "command": "go run .",
          "cwd": "apps/gateway"
        }
      }
    }
  }