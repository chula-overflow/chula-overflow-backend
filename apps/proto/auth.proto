syntax = "proto3";

package auth;

option go_package = "../gateway/src/proto";

service Auth {
  rpc Login (AuthRequest) returns (AuthResponse) {}
}

message AuthRequest {
  string name = 1;
}
  
message AuthResponse {
  string token = 1;
}